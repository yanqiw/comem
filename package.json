{
  "name": "coordination-memory-console",
  "private": true,
  "version": "0.2.4",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "test": "vitest run",
    "check": "npm run test && npm run test:pages && npm run build && npm run docs:build",
    "test:pages": "node --test tests/pages-contract.test.mjs",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.0.0",
    "svelte": "^5.0.0",
    "vite": "^7.0.0",
    "vitepress": "^1.6.4",
    "vitest": "^3.0.0"
  }
}
