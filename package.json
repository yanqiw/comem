{
  "name": "coordination-memory-console",
  "private": true,
  "version": "0.2.5",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "test": "vitest run",
    "check": "npm run test && npm run build"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.0.0",
    "svelte": "^5.0.0",
    "vite": "^7.0.0",
    "vitest": "^3.0.0"
  }
}
